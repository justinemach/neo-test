// neo tester — runtime config ("env" for a no-build static app).
//
// Leave these blank to run in LOCAL mode (saves to your browser's localStorage).
// Paste your Supabase project values to switch to CLOUD mode — no rebuild needed,
// just edit + redeploy. Find them in: Supabase dashboard → Project Settings → API.
window.NEO_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",
};
